# =============================================================================
# frontend/.env.example — Vite Environment Variables
# =============================================================================
# Copy to frontend/.env for local development
# Copy to frontend/.env.production for production builds
# =============================================================================

# Backend API URL
# Dev:        http://localhost:8000
# Production: https://api.yourdomain.com/api   (routed through nginx /api/)
VITE_API_BASE_URL=http://localhost:8000
